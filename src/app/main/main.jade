.container.main

  touch

  socket

  filter(filter="view.filter" user="user" on-update="recieveFilter(filter)")

  menu(user="user")

  div(ui-view="slideshow" user="user" view="view" on-update="recieveFilter(filter)")

  div(ui-view)

  slides(view="view" user="user")

  #bottom
    pager(filter="view.filter" on-update="recieveFilter(filter)")

    .admin(ng-if="user.role=='admin'")
      admin(user="user" view="view" touch="touch")

  footer.footer
    .container
      p
        | homemademess.com
        = ' | '
        a(href='https://github.com/markisatacomputer/homemademess') github
