.container.main

  .admin(ng-if="user.role=='admin'")
    admin(user="user" view="view")

  .filter(ng-if="showFilters")
    searchbar(filter="view.filter"  on-update="recieveFilter(filter)")

  menu(user="user")

  div(ui-view="slideshow")

  div(ui-view)

  slides(view="view" user="user")

  pager(filter="view.filter" on-update="recieveFilter(filter)")

  footer.footer
    .container
      p
        | homemademess.com
        = ' | '
        a(href='https://github.com/markisatacomputer/homemademess') github
