.container.main

  div(ui-view)

  searchbar(filter="view.filter"  on-update="recieveFilter(filter)" ng-show="showFilters")

  div(ui-view="slideshow")

  menu(user="user" on-update-scope="recieveScopeUpdate(update)" on-broadcast-action="recieveMenuAction(action)")

  slides

  admin(user="user" view="view" on-update="recieveView(view)")

  pager(filter="view.filter" on-update="recieveFilter(filter)")

  footer.footer
    .container
      p
        | homemademess.com
        = ' | '
        a(href='https://github.com/markisatacomputer/homemademess') github
